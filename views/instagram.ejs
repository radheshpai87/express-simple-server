<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Profile - @<%= profile.name %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .stats {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }

        .buttons {
            margin-bottom: 20px;
        }

        button {
            background-color: #0095f6;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
            margin-right: 10px;
        }

        .gallery {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
        }

        .post {
            position: relative;
        }

        .post img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .post-stats {
            position: absolute;
            bottom: 10px;
            left: 10px;
            color: white;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.7);
        }
    </style>
</head>

<body>
    <div class="header">
        <h2>This page belongs to @<%= profile.name %>
        </h2>
    </div>

    <div class="stats">
        <h3>Followers: <%= profile.followers %>
        </h3>
        <h3>Following: <%= profile.following %>
        </h3>
    </div>

    <div class="buttons">
        <button>Follow</button>
        <button>Message</button>
    </div>

    <div class="gallery">
        <% profile.posts.forEach(post=> { %>
            <div class="post">
                <img src="<%= post.image %>" alt="Post by <%= profile.name %>">
                <div class="post-stats">
                    ❤️ <%= post.likes %> &nbsp;&nbsp; 💬 <%= post.comments %>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>